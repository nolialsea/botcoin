const main = require('main');
//Yeah, nothing else.
//Nodejs modules are singleton if you import them from the same location
//So it's just an ugly way to avoid some problems with database and irc client